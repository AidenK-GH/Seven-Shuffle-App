<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="15dp"
    android:layout_margin="25dp"
    android:orientation="vertical"
    style="@style/layoutStyleBackgroundColor"
    >
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_margin="0dp">

        <TextView
            style="@style/textViewStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Settings" />

        <Button
            android:id="@+id/exit_button"
            style="@style/buttonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="X" />
    </LinearLayout>


    <TextView
        android:text="Input Options:"
        style="@style/textViewStyle"
        />

    <Button
        android:id="@+id/toggle_input_mode_btn"
        style="@style/buttonStyle"
        android:text="Switch Input Mode between Buttons and Keyboard"
        />

    <TextView
        android:text="Theme:"
        style="@style/textViewStyle"
        />

    <Spinner
        android:id="@+id/themeSpinner"
        style="@style/spinnerStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:entries="@array/theme_options" />

    <TextView
        android:text="Changing the theme will make the game restart."
        style="@style/textViewStyle"
        />

    <TextView
        android:id="@+id/instractions_textview"
        style="@style/textViewStyle"
        android:text="@string/Instructions"
        />

</LinearLayout>